#include "catch2/catch2.hpp"
#include "suitcase.h"

/*
TEST_CASE("Inizialized", "[suitcase]") {

    NatsSuitcase* device = nats_init();
    nats_setparameters(1, 1, 1, 1, 1, 1, device);

    REQUIRE(device->pole.h_pole == 1);

}
*/

   /* test
    string total = nats_read_from_file();
    int a = 0;
    float b = nats_parse(total, "rotate(", ", ", a);
    cout << b << endl;
   */
